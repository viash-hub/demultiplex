name: demultiplex.vsh
description: |
  Demultiplexing pipeline
license: MIT
keywords: [bioinformatics, sequence, demultiplexing, pipeline]
links:
  issue_tracker: https://github.com/viash-hub/demultiplex.vsh/issues
  repository: https://github.com/viash-hub/demultiplex.vsh

viash_version: 0.9.0-RC2
# config_mods: |
#   .platforms[.type == 'docker'].target_organization := 'viash-hub'
#   .platforms[.type == 'docker'].target_registry := 'viash-hub.com:5050'
#   .platforms[.type == 'nextflow'].directives.tag := '$id'
